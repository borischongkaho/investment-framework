[project]
name = "boris-trade"
version = "5.0.0"
description = "Investment framework — ACCT6111E v5 hardened"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["framework"]
